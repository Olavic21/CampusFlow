# PostgreSQL + Redis pour CampusFlow (Docker, Oracle Cloud)
# Copier vers deploy/oracle/.env.db (ignoré par Git) et remplacer les valeurs.
POSTGRES_USER=campusflow
POSTGRES_PASSWORD=<DB_PASSWORD>
POSTGRES_DB=campusflow
